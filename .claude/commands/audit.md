# Audit Command

Use this command to update vulnerabilities and run an audit for the project.

```bash
git pull
npm audit fix --force
npm audit
npm run audit
```

If you use yarn:

```bash
yarn install --check-files
yarn audit
```
